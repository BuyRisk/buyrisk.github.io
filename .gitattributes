# Normalize line endings: LF in the repo, auto on checkout per-platform.
* text=auto eol=lf
*.png binary
*.jpg binary
*.svg text

# Primary-source datasets are stored verbatim — no line-ending normalization,
# so the bytes match exactly what was downloaded from the publisher.
data/sources/** -text
