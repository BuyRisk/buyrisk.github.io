# Commenting log

What has been commented, when, and how we checked the code actually works
first. Plain language on purpose.

| Date | File(s) | How we verified it runs | Notes |
|---|---|---|---|
| 2026-08-17 | scripts/lib/data-paths.mjs, parse-crsp.mjs, parse-dta.mjs, parse-fred.mjs, parse-french.mjs, read-xlsx.mjs, scripts/inspect-sources.mjs | Ran `npm run data:inspect` — it read every raw source (French, Damodaran, Shiller, FRED, JST) and finished cleanly, exit 0, before and after the edits. | These files already had thorough headers and inline notes, so we only added two small clarifications: what Stata's "missing number" cutoffs (1e37, 8.9e307) mean in parse-dta, and that FRED writes a missing value as a period in parse-fred. Nothing else needed. |
| 2026-08-17 | All 20 reducers in scripts/ (reduce-active-share, reduce-asset-stats, reduce-behavior-gap, reduce-cape, reduce-capm, reduce-crsp-diversification, reduce-crsp-superstock, reduce-damodaran-returns, reduce-debt, reduce-factor-premia, reduce-fees, reduce-fred-yields, reduce-french-daily, reduce-global-equity, reduce-housing, reduce-inflation, reduce-shiller-monthly, reduce-sp500-concentration, reduce-spiva, reduce-ssa) | Ran every one via its npm data: script; all finished cleanly (exit 0) and every generated file under src/data/generated came out byte-for-byte identical (git saw no changes). The three files we touched were re-run afterward — still clean, still identical output. | Every reducer already had a full header (purpose, source, citation, how to run) and inline notes on the finance math. We added just three clarifications: how Shiller encodes dates as year-dot-month decimals (in reduce-cape and reduce-shiller-monthly), and why behavior-gap drops funds showing returns beyond ±60% a year (almost certainly data glitches, not real funds). |
| 2026-08-17 | scripts/pull/crsp-monthly.py | Read-verified only. Running it needs WRDS (Wharton Research Data Services) login credentials we don't have, so it was not executed. What it does is confirmed by reading: it downloads CRSP monthly stock returns and writes a CSV plus a provenance manifest. | Already carries a thorough header docstring and inline notes (including the two WRDS query formats and the delisting-return adjustment), so no comments were added. Left as-is per the rule that unverified code gets no new inline comments. |
| 2026-08-18 | All React components in src/components (55 .tsx files, reviewed one by one) plus the five .astro components (Header, Footer, FormattedDate, NextTool, and the already-commented small ones) | `npm run build` (which typechecks every file and builds all 38 pages) passed before the edits and again after them. | Nearly every component already carried a full header and inline notes on its finance math. We filled the gaps we found: a missing file header on PortfolioLab (the biggest tool) plus notes on its simulation constants; headers on the four plain .astro components; and short explanations of judgment-call numbers — the emergency-fund month formula and buffers, the future-value-of-monthly-savings formula (NetWorthLab, SavingsGoalLab), the tax-drag model's basis and turnover logic (TaxDragLab), the market-game's drift and volatility (EmhLab), and the risk-questionnaire's mismatch flag. |
| 2026-08-18 | src/layouts (BaseLayout, BlogPost) and all 40 pages under src/pages | `npm run build` — full typecheck and 38-page build — passed before and after the edits. Each header's claims (which data file a page reads, which tool it hosts) were checked against the page's actual imports. | Light touch, as these are mostly thin page shells: every layout and page got a one-or-two-line header saying what the page is and what it hosts or reads. Pages that already had inline notes on their math (like the historical-returns real-return calculation) were left as they were. |
